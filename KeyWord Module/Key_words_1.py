from keyword import iskeyword,kwlist
# only two functions in keyword module

# help(iskeyword)
# help(kwlist)
# iskeyword(s)
# Return True if s is a Python keyword.

# kwlist # is variable mean assign List
# Sequence containing all the keywords defined for
# the interpreter. If any keywords are defined to only
# be active when particular __future__ statements are
# in effect, these will be included as well.

print('keywords (reserve)in of python')
print(kwlist)