from math import perm

# help(perm) # Must check

# Evaluates to n! / (n - k)! when k <= n and evaluates
# to zero when k > n.

Permutation = perm(4,2)
print(Permutation)


# and also check  the itertools library Permutation topic