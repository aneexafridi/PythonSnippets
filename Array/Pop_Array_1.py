# this method is used to remove last
# element from the existing array
# Syntax:- Array_Name.pop()

from array import *
Array = array('i',[1,2,3,4,5])
print(Array)
print("After Pop the Element from the Array")
Array.pop()
print(Array)