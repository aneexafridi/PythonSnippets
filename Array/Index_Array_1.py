# this method returns position number
# of first occurence of given element
# in the Array. If it doesn't found
# the element,shows valueError
# Syntax:-  array_Name.index(element)
from array import *
Array = array('i',[1,2,3,4])
print(Array)
print("index of 4 is: ",Array.index(4))