from statistics import median_high
from string import ascii_uppercase
# help(median_high)
List = ['A', 'B', 'C', 'D', 'E', 'F', 'G','H']
# List = [ascii_uppercase[u] for u in range(8)]
print(List)

print(f'Median High: {median_high(List)}')

# also same work for Integers
